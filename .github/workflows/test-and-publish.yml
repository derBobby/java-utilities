name: Maven test and publish

on:
  pull_request:
    branches: [ main ]
    types:
      - closed
  push:
    branches: [ main ]

jobs:
  mvn-publish:
    uses: ./.github/workflows/callable-maven.yml
    with:
      mvn-stage: deploy